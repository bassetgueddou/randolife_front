import React from 'react';

const CreerActivite = () => {
    return (
        <div>
            <h2>Créer une nouvelle activité</h2>
            {/* Ici, nous ajouterons un formulaire pour créer une nouvelle activité */}
        </div>
    );
};

export default CreerActivite;
